L'application se lance à partir de app.py

features and roles to install on server : IIS , CGI


Modification à faire :
	app.run(IP ou nom-machine,port=num-port,debug = True,use_reloader = False,threaded=True)

	pathDB=chemin-db
